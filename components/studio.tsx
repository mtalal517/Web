import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { Photo, Reveal } from './visuals';
import { mediaLinks, services } from '@/lib/portfolio';
export function StudioNote(){return <section className="studio-note section-pad"><Reveal className="studio-photo"><Photo src={mediaLinks.studioPortrait} alt="A photographer holding a camera outdoors" sizes="(max-width:700px) 85vw, 35vw"/><span className="eyebrow image-footnote">ON THE OTHER SIDE OF THE FRAME</span></Reveal><Reveal className="studio-copy"><span className="eyebrow">BEHIND THE LENS</span><h2>Always looking.<br/><em>Never just seeing.</em></h2><p>We’re a small photography and film studio drawn to real people, honest places, and the details you almost miss.</p><p>From a room full of celebration to a quiet moment alone, we make images that feel like being there.</p><Link className="text-link" href="/about">Meet the studio <ArrowUpRight size={17}/></Link></Reveal></section>;}
export function ServiceList(){return <div className="service-list">{services.map((s,i)=><Link key={s} href={`/contact?service=${encodeURIComponent(s)}`}><span className="index">0{i+1}</span><span>{s}</span><ArrowUpRight size={20}/></Link>)}</div>;}
export function ContactScene(){return <section className="contact-scene section-pad"><span className="eyebrow">EVERY GOOD STORY STARTS WITH A CONVERSATION</span><h2>Let’s create something<br/><em>worth remembering.</em></h2><Link className="contact-link" href="/contact">Tell us what you have in mind <ArrowUpRight/></Link><div className="contact-location">BASED IN ISLAMABAD <span>AVAILABLE FOR STORIES EVERYWHERE</span></div></section>;}
