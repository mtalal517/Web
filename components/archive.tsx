import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { projects, categories, Category } from '@/lib/portfolio';
import { Photo, Reveal } from './visuals';
export function Archive({category}:{category?:Category}){const items=projects.filter(p=>!category||p.category===category);return <><div className="page-intro section-pad"><span className="eyebrow">THE ARCHIVE / 2025—2026</span><h1>{category?<>{category}<em>.</em></>:<>A collected <em>view.</em></>}</h1><p>{category?'A closer look. A different perspective. Explore the stories.':'People, places, and the moments in between. A selection of photography and film from our world.'}</p><nav className="filter-nav" aria-label="Portfolio categories"><Link href="/work" aria-current={!category?'page':undefined}>All work ({projects.length})</Link>{categories.map(c=><Link key={c} href={`/work/category/${c.toLowerCase()}`} aria-current={category===c?'page':undefined}>{c}</Link>)}</nav></div><div className="archive-grid section-pad">{items.map(p=><Reveal className="project-preview" key={p.slug}><Link href={`/work/${p.slug}`} data-cursor="VIEW"><Photo src={p.coverImage} alt={p.alt}/><div className="project-caption"><h3>{p.title}</h3><span>{p.category.toUpperCase()} / {p.year}</span><ArrowUpRight size={18}/></div></Link></Reveal>)}</div></>;}
