'use client';
import { useEffect, useState } from 'react';
import { ArrowUpRight } from 'lucide-react';
import { services, studio } from '@/lib/portfolio';
export function InquiryForm(){const [service,setService]=useState('');const [ready,setReady]=useState(false);useEffect(()=>{const value=new URLSearchParams(window.location.search).get('service');if(value&&services.includes(value))setService(value);},[]);return <form className="inquiry-form" onSubmit={e=>{e.preventDefault();const data=new FormData(e.currentTarget);const body=`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nService: ${service}\n\n${data.get('message')}`;window.location.href=`mailto:${studio.email}?subject=${encodeURIComponent(`A new story — ${service||'Studio inquiry'}`)}&body=${encodeURIComponent(body)}`;setReady(true);}}><label>Your name<input autoComplete="name" name="name" required placeholder="How should we call you?"/></label><label>Email address<input name="email" type="email" autoComplete="email" required placeholder="you@example.com"/></label><label>What are you imagining?<select name="service" value={service} onChange={e=>setService(e.target.value)} required><option value="" disabled>Select a service</option>{services.map(s=><option key={s}>{s}</option>)}</select></label><label>A little about your story<textarea name="message" required placeholder="The place, the date, the idea. We’re listening."/></label><button type="submit">Prepare an email <ArrowUpRight size={18}/></button><p className="form-note" role="status">{ready?'Your email draft is ready in your email app. Review it and press send there. If no app opened, copy your message into your preferred email service.':'Opens your email app with your inquiry. Nothing is sent automatically.'}</p></form>;}
